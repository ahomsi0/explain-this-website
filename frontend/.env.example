VITE_API_URL=http://localhost:8080
VITE_USE_MOCK=false
# Optional: Google Analytics measurement ID. Analytics loads only after
# explicit consent; leave empty to fall back to the built-in default ID.
VITE_GA_MEASUREMENT_ID=
